<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ subject }}</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Atkinson Hyperlegible', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background-color: hsl(230, 25%, 92%);
      color: hsl(230, 24%, 18%);
      line-height: 1.5;
    }
    .email-container {
      max-width: 600px;
      margin: 40px auto;
      background-color: hsl(0, 0%, 100%);
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0px 3px 2px rgba(0, 0, 0, 0.016), 0px 5px 4px rgba(0, 0, 0, 0.024), 0px 25px 20px rgba(0, 0, 0, 0.04);
    }
    .email-header {
      background: linear-gradient(135deg, hsl(210, 100%, 43%) 0%, hsl(313, 100%, 44%) 100%);
      padding: 40px 32px;
      text-align: center;
    }
    .email-header h1 {
      margin: 0;
      color: hsl(0, 0%, 100%);
      font-size: 28px;
      font-weight: 700;
    }
    .email-body {
      padding: 40px 32px;
    }
    .email-body p {
      margin: 0 0 16px 0;
      font-size: 16px;
      color: hsl(230, 18%, 34%);
    }
    .email-button {
      display: inline-block;
      margin: 24px 0;
      padding: 14px 32px;
      background-color: hsl(210, 100%, 43%);
      color: hsl(0, 0%, 100%) !important;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 700;
      font-size: 16px;
      text-transform: uppercase;
      transition: background-color 0.3s;
    }
    .email-button:hover {
      background-color: hsl(210, 100%, 38%);
    }
    .email-footer {
      padding: 32px;
      background-color: hsl(230, 25%, 96%);
      border-top: 1px solid hsl(230, 19%, 78%);
      font-size: 14px;
      color: hsl(230, 13%, 54%);
    }
    .email-footer p {
      margin: 0 0 8px 0;
    }
    .email-link {
      color: hsl(210, 100%, 43%);
      word-break: break-all;
    }
    .expiry-notice {
      margin-top: 24px;
      padding: 16px;
      background-color: hsl(48, 75%, 95%);
      border-left: 4px solid hsl(48, 75%, 50%);
      border-radius: 4px;
      font-size: 14px;
      color: hsl(230, 18%, 34%);
    }
  </style>
</head>
<body>
  <div class="email-container">
    <div class="email-header">
      <h1>{{ appName }}</h1>
    </div>
    <div class="email-body">
      <p><strong>{{ greeting }}</strong></p>
      <p>{{ intro }}</p>

      <center>
        <a href="{{ resetLink }}" class="email-button">
          {{ action }}
        </a>
      </center>

      <div class="expiry-notice">
        <p><strong>⚠️ {{ expiry }}</strong></p>
      </div>

      <p>{{ outro }}</p>
    </div>
    <div class="email-footer">
      <p>{{ footer }}</p>
      <p><a href="{{ resetLink }}" class="email-link">{{ resetLink }}</a></p>
    </div>
  </div>
</body>
</html>
